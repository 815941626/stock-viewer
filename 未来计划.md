# 未来计划

> 待办设计清单。每项标注状态与前置条件，敲定后再动手。

## 1. 半年线视角（拥挤度扩展 + 主线识别）

**状态**：讨论中（2026-08-09 提出），等开盘观察几天市场情况再拍板。

**动机**：现有拥挤度三分量全是短周期（当日小单/宽度、60日/YTD 涨幅），
缺中期趋势位置。"整个市场主推什么"需要半年尺度。

**两个用法（分开实现）**：

### A. 半年线乖离 → 拥挤度第四分量
- 板块指数 vs MA120 的乖离率：贴线/刚上穿 = 中期空间未涨掉（新主线起点候选）；
  乖离 30%+ = 中期透支（鱼尾段）
- 与动量的最佳象限：**v5 加速 + 贴半年线** = 主线新启动；v5 加速 + 高乖离 = 赶顶
- 进 `CONGESTION.weights` 第四位，架构已预留

### B. 半年涨幅截面排名 → 主线标签
- 池内 40 板块按半年涨幅排序，头部 = 市场主战场，给板块打"主线"标签
- 与动量榜各管各的：主线板块可能短期斜率已平（霸榜），工具要区分的
  恰是"主线上的新启动"

**数据路线**（已探明）：
- 板块价格日K在本IP不可用（push2his 断连 / push2delay 返空）
- 绕法：fflow 日K（push2his，间歇可用）带收盘价列，上限 120 天 ≈ 正好一条半年线
- 方案：每日低频拉一次 + 重试 + 磁盘缓存（同池子 baseline 思路）；
  拉不到沿用旧缓存，连续失败降级显示 -

**待拍板三问**：
1. 用法 A 和 B 都要，还是先做一个？（建议都要）
2. 乖离"过热"阈值：30% 还是 50%？（用交易体感定，不猜）
3. 接受 push2his 每日低频拉的脆弱性？

## 2. 成交额过热分量（拥挤度补全）

依赖板块日K源，同上路线解决后一并补上。

## 3. 拥挤度降权（观察期后）

位置徽标跑两三周观察日志（`backend/logs/congestion_*.jsonl`），
人工复盘"标了 70% 的板块 T+1/T+3 是否真退潮"，有区分度再谈进排名。

## 4. accel 浮点死区

v5/v15 近零时加速徽标抖动，加死区阈值（已知粗边，顺手活）。

## 5. 筹码结构四象限：主力 vs 小单背离（设计已定，下次实现）

**状态**：设计敲定（2026-08-10），未实现。
**起因**：2026-08-10 开盘实测——数据中心概念主力 -88.8亿 出货、小单 +66.6亿 接盘
（接盘强度 0.75），教科书式派发形态。用户拍板：做主力/小单对比逻辑。

**定位**：动量回答"油门加多快"，拥挤度回答"涨到哪一段"，
筹码结构回答"**现在谁在买**"——三者是同一生命周期的不同截面。
派发象限就是"已经晚了"的直接定义，本模块是工具的防守件（长边：别去追）。

### 5.1 数据基础（全部已有，零新增请求）

| 资产 | 现状 | 本模块用途 |
|---|---|---|
| f62/f84/f6/f184（主力/小单净流入、成交额、主力净占比） | 已在 5 秒 ulist 轮询 | 象限判定 |
| `_histories`（主力累计流入，5 秒采样，盘内闸） | 动量 v5/v15 在用 | 主力侧导数已有 |
| `_small_histories`（小单净占比，5 秒采样） | 仅观察输出 | 算小单侧斜率 s5/s15 |
| `in_trading_session` 闸 / 观察日志机制 | 已有 | 复用 |

### 5.2 状态空间与判定（P1）

全部走**净占比空间**（消除日内时点影响）：
`main_pct = f184`，`small_pct = f84 ÷ f6 × 100`

| 条件 | 象限 | 生命周期含义 |
|---|---|---|
| main_pct > +X 且 small_pct < -Y | 吸筹 | 早期，主力买散户卖（最好买点区域） |
| main_pct > +X 且 small_pct > +Y | 共振涌入 | 主升中段 |
| main_pct < -X 且 small_pct > +Y | **派发** | 晚期，主力出货散户接盘（危险） |
| main_pct < -X 且 small_pct < -Y | 双逃 | 崩后/出清 |
| 其余 | 中性 | — |

- X = Y = 1.5% 为初值锚点，靠日志校准（普通交易日 ±1~2%，
  2026-08-10 数据中心为 -6.5%/+4.9% 的极端派发日）
- **迟滞带防抖**：进入阈值 1.5%、退出阈值 1.0%——5 秒轮询下
  边界附近的标签不能来回跳
- **开盘预热**：前 15 分钟不定象限（比值极端且噪声大），显示"早盘"

连续量（比标签更值钱）：
- **接盘强度** = 小单净流入 ÷ |主力净流入|（派发象限内）
  ——0.75 = 散户接走主力抛盘的 75%；0.3 和 0.9 的派发是两个世界
- **吸筹强度** = 主力净流入 ÷ |小单净流入|（吸筹象限内），对称定义

### 5.3 结构翻转检测（P2，真正的 alpha）

静态象限是后视镜；**日内"吸筹→派发"的翻转时点**才是信号：

- s5/s15 = 小单净占比的 5/15 分钟斜率（百分点/分钟），
  直接复用 `slope_per_min`，数据取 `_small_histories`
- 翻转信号：v5 转负（主力侧动量已有此判定）且 s5 > 0
  且持续 ≥ 3 分钟 → "派发翻转进行中"
- 防误报：单样本穿越不触发，需持续窗确认；翻转事件写入观察日志

### 5.4 与动量/拥挤度的决策矩阵（最终形态）

| 动量 | 拥挤度 | 筹码结构 | 解读 |
|---|---|---|---|
| v5 加速 | 低 | 吸筹 | **启动确认（最佳象限）** |
| v5 > 0 | 高 | 共振涌入 | 主升中段，参与但盯翻转 |
| v5 > 0 | 高 | **派发** | **高位派发伪装（最危险：价格还在涨、结构已翻转）** |
| v5 减速 | 高 | 派发 | 顶部确认，别碰 |
| v5 < 0 | 低 | 双逃 | 出清中，不抄底 |

矩阵第三行是本工具要抓的核心陷阱——"看到时已经晚了"的典型：
涨幅和动量还好看，但筹码已经在从主力流向散户。

### 5.5 验证与校准（同拥挤度的观察期哲学）

- 观察日志（congestion_*.jsonl）扩字段：
  `main_pct / small_pct / pattern / absorption / s5`
- 校准标准：派发标签后的 T+1/T+3 涨跌 vs 动量榜均值基准，
  算命中率——跑不赢基准就降为纯展示
- 阈值 X/Y 从日志分布校准（如历史 |main_pct| 的 90 分位）
- 抖动监控：单板块单日标签翻转次数超阈值 → 加宽迟滞带

### 5.6 已知坑（实现时逐条处理）

1. **大单卖 ≠ 出货**：ETF 申赎、指数调仓、大宗交易都体现为大单卖出
   ——标签是警告不是判决，单日形态需持续性确认
2. 拆单污染：机构拆单漏进小单、大户单笔被当主力（与拥挤度同源 caveat）
3. 盘外冻结值会造出假象限：必须挂 `in_trading_session` 闸
4. 跨板块重复计算不影响本模块（占比是板块内自洽的）
5. 这是防守件：意义是"别追高"，别指望它给卖出点精度

### 5.7 实现分期

- **P1（半天量）**：`chipflow.py` 纯函数（象限 + 强度 + 迟滞 + 自校验）
  → `/api/sectors` 附 `flow_pattern={pattern, absorption, main_pct, small_pct}`
  → 看板拥挤度徽标旁加象限角标（派发=红、吸筹=绿）→ 观察日志扩字段
- **P2（观察期后）**：s5/s15 小单斜率 + 翻转检测 → 详情页轨迹提示
- **P3（远期）**：个股级筹码结构——`/api/stock_quotes` 补 f84（零成本），
  看"板块派发但个别龙头仍吸筹"= 题材未死、龙头切换的轮动信号
